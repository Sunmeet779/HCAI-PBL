runtime: python311  # or python310
entrypoint: gunicorn -b :$PORT pbl.wsgi

handlers:
- url: /static
  static_dir: static/

- url: /.*
  script: auto

env_variables:
  DJANGO_SETTINGS_MODULE: pbl.settings
